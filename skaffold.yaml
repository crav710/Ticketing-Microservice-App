apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: crav710/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
    - image: crav710/client
      context: client
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "**/*.js"
            dest: .
    - image: crav710/tickets
      context: tickets
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
    - image: crav710/orders
      context: orders
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
    - image: crav710/expiration
      context: expiration
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
    - image: crav710/payments
      context: payments
      docker:
        dockerfile: Dockerfile
        noCache: true
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
